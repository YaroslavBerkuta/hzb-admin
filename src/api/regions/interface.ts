import { IDistributors } from "../../typing/interfaces/distributors.interface";

/* eslint-disable @typescript-eslint/no-explicit-any */
export interface IResDistributorsList {
  count: number;
  items: IDistributors[];
}
